---
- name: Validate inventory and injected token inputs without contacting hosts
  hosts: k3s_servers:k3s_agents
  gather_facts: false
  any_errors_fatal: true
  tasks:
    - name: Evaluate controller-side input contract only
      ansible.builtin.import_tasks: tasks/validate-input.yml
